# PostgreSQL connection (choose one of the following)
# Full connection string (recommended)
ConnectionStrings__DefaultConnection=Host=your-host;Port=5432;Database=your-db;Username=your-user;Password=your-pass;SslMode=Require;TrustServerCertificate=true

# Or a single DATABASE_URL in URL form
# DATABASE_URL=postgresql://your-user:your-pass@your-host:5432/your-db

# JWT settings
JwtSettings__Key=your-very-secret-key
JwtSettings__Issuer=your-issuer
JwtSettings__Audience=your-audience

# Optional: run migrations in entrypoint
# RUN_MIGRATIONS=true

# Keycloak Configuration
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=review-realm
KEYCLOAK_CLIENT_ID=review-api
KEYCLOAK_CLIENT_SECRET=your-client-secret-here
